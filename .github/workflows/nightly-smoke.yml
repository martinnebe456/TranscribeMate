name: Nightly Smoke

on:
  workflow_dispatch:
  schedule:
    - cron: "30 2 * * *"

permissions:
  contents: read

jobs:
  backend_tests:
    uses: ./.github/workflows/ci-backend.yml

  frontend_build:
    uses: ./.github/workflows/ci-frontend.yml

  stdio_smoke:
    uses: ./.github/workflows/ci-stdio-smoke.yml
